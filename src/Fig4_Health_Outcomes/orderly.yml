script: script.R

parameters:
  deterministic:
      default: false
  assumptions:
    default: "central"
  vaccine_assumptions:
    default: "baseline"
  param_iterations:
    default: 100
  
  
artefacts:
  - staticgraph:
      description: supplementary figures
      filenames:
        - Fig4.png
        - Fig4.pdf
  - data:
      description: Already processed data
      filenames:
        - Figure_dataframes/simulation_data.rds
        - Figure_dataframes/england_data.rds

packages:
  - sircovid
  - spimalot
  - tidyr
  - dplyr
  - forcats
  - magrittr
  - ggplot2
  - scales
  - cowplot
  
global_resources:
  global_util.R: rtm_inference/util_new.R
  
resources:
  - plotting_functions.R

depends:
  - 01_parsed_data:
      id: latest
      use:
        england_region_data.csv: outputs/england_region_data.csv
  - 02_parameters:
      id: latest(parameter:assumptions == assumptions && parameter:deterministic == deterministic && parameter:population_assumptions == "ONS_NHS_region_principal" && parameter:population_year == 2047 && parameter:vaccine_assumptions == vaccine_assumptions)
      use:
        dependencies/baseline_2047.rds: parameters_base.rds
  - 02_parameters:
      id: latest(parameter:assumptions == assumptions && parameter:deterministic == deterministic && parameter:population_assumptions == "ONS_NHS_region_principal" && parameter:population_year == 2037 && parameter:vaccine_assumptions == vaccine_assumptions)
      use:
        dependencies/baseline_2037.rds: parameters_base.rds
  - 02_parameters:
      id: latest(parameter:assumptions == assumptions && parameter:deterministic == deterministic && parameter:population_assumptions == "ONS_NHS_region_principal" && parameter:population_year == 2027 && parameter:vaccine_assumptions == vaccine_assumptions)
      use:
        dependencies/baseline_2027.rds: parameters_base.rds
  - 06_parsed_simulations:
      id: latest(parameter:assumptions == assumptions && parameter:param_iterations == param_iterations && parameter:deterministic == deterministic && parameter:population_assumptions == "ONS_NHS_region_principal" && parameter:population_year == 2047 && parameter:vaccine_assumptions == vaccine_assumptions)
      use:
        dependencies/2047_output_dataframe.rds: combined_output_dataframe.rds
        dependencies/2047_index_names.rds: index_names.rds
  - 06_parsed_simulations:
      id: latest(parameter:assumptions == assumptions && parameter:param_iterations == param_iterations && parameter:deterministic == deterministic && parameter:population_assumptions == "ONS_NHS_region_principal" && parameter:population_year == 2037 && parameter:vaccine_assumptions == vaccine_assumptions)
      use:
        dependencies/2037_output_dataframe.rds: combined_output_dataframe.rds
        dependencies/2037_index_names.rds: index_names.rds
  - 06_parsed_simulations:
      id: latest(parameter:assumptions == assumptions && parameter:param_iterations == param_iterations && parameter:deterministic == deterministic && parameter:population_assumptions == "ONS_NHS_region_principal" && parameter:population_year == 2027 && parameter:vaccine_assumptions == vaccine_assumptions)
      use:
        dependencies/2027_output_dataframe.rds: combined_output_dataframe.rds
        dependencies/2027_index_names.rds: index_names.rds
  - 06_parsed_simulations:
      id: latest(parameter:assumptions == assumptions && parameter:param_iterations == param_iterations && parameter:deterministic == deterministic && parameter:population_assumptions == "rtm_baseline" && parameter:population_year == 2019 && parameter:vaccine_assumptions == "baseline")
      use:
        dependencies/baseline_combined_output_dataframe.rds: combined_output_dataframe.rds
        dependencies/baseline_index_names.rds: index_names.rds